import { PriceBetweenPipe } from './price-between.pipe';

describe('PriceBetweenPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceBetweenPipe();
    expect(pipe).toBeTruthy();
  });
});
